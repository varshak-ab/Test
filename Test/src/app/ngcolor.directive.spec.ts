import { NgcolorDirective } from './ngcolor.directive';

describe('NgcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new NgcolorDirective();
    expect(directive).toBeTruthy();
  });
});
